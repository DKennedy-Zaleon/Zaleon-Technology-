services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: zaleon
      POSTGRES_USER: zaleon
      POSTGRES_PASSWORD: change-me-in-development
    ports:
      - "5432:5432"
    volumes:
      - zaleon_pg:/var/lib/postgresql/data

volumes:
  zaleon_pg:
